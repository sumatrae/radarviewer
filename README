主要功能：
1.提供多camera选择和参数读取、配置功能,因摄像头写入参数后可以保存参数，因此无需单独保存参数，但需要再软件启动后读取选择摄像头的参数;
2.提供radar配置接口，radar使用串口配置，串口参数可以配置和保存;
3.提供软件及公司介绍；
4.主界面提供视频实时显示，可切换到拍照界面，主界面对计算得到的障碍物像素坐标，用红点标记，并用绿色矩形框标识一定范围，防止误差导致的标识不准确；
5.主界面视频画面提供可拖动的红线标识框，识别在该区域有障碍物出现时，才进行定时拍照，并将照片存入硬盘记录；
6.拍照界面，提供存图的浏览功能，根据时间戳顺序浏览图片，界面默认显示最后一张图片，图片上带拍照时的标识信息；
7.为简化模型，假设雷达和摄像头坐标系完全平行，因此只存在平移矩阵
8.复杂模型，通过初步确定平移矩阵后，通过动态调整角度等参数，使图像画面目标矩形框到正确位置
